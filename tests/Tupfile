include_rules
CXXFLAGS += -isystem $(BOOST_INCLUDE) -I$(RECKLESS_INCLUDE) -lpthread
LDFLAGS += -lpthread -L$(RECKLESS_LIB) -lreckless
: foreach *.cpp |> !cxx |>
: foreach *.$(OBJEXT) | $(RECKLESS_LIB)/$(LIBPREFIX)reckless.$(LIBEXT) |> !ld |> %B
