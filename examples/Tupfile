include_rules
CXXFLAGS += -isystem $(BOOST_INCLUDE) -I$(ASYNCLOG_INCLUDE)
LDFLAGS += -L$(ASYNCLOG_LIB) -lasynclog
: foreach *.cpp |> !cxx |>
: foreach *.o | $(ASYNCLOG_LIB)/libasynclog.a |> !ld |> %B
