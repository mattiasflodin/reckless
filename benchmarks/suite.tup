: periodic_calls.cpp |> !pcxxld |> $(LIB)_periodic_calls
: mandelbrot.cpp |> !pcxx |> $(LIB)_%B.o

THREADS=1
include suite_per_thread.tup
THREADS=2
include suite_per_thread.tup
THREADS=3
include suite_per_thread.tup
THREADS=4
include suite_per_thread.tup
